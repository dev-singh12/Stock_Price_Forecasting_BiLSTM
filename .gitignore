# Python
__pycache__/
*.py[cod]
*.egg-info/
.pytest_cache/
.hypothesis/

# Environments
.env
venv/
.venv/

# Editors
.vscode/
.DS_Store

# Jupyter
.ipynb_checkpoints/

# Kiro AI orchestration — useful locally, not for public repo
.kiro/

# --- Runtime-generated data files (not committed) ---
data/aapl_rolling.csv
data/aapl_features.csv
data/*.tmp

# --- Runtime log files ---
logs/*.jsonl

# --- Versioned model artifacts (generated by retrain pipeline) ---
# bilstm_stock_model.keras is intentionally NOT ignored — it is the
# legacy reference artifact and should remain in the repo.
models/model_v*.keras
models/scaler_v*.pkl
models/scaler_v*_meta.json
models/*.tmp

